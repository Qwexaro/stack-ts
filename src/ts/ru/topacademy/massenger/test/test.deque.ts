import { Deque } from "../deque/Deque.ts";

const chatHistory = new Deque();

// chatHistory.addMessage({ user: 'Alice', text: 'Привет!' });
// chatHistory.addMessage({ user: 'Bob', text: 'Здравствуйте!' });